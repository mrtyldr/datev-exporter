/**
 * Dependency-free buffered and forward-only serialization of fixed column headings and DATEV
 * Buchungsstapel booking rows. The lean package does not emit an EXTF management record.
 */
package io.github.mrtyldr.datev.plain;
